<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li> <li><%= link_to "🗑", task_path(task), method: :delete, data: {confirm: "delete task?"} %></li>
  <% end %>
</ul>
<%= link_to "+ Add a new task", tasks_new_path%>
